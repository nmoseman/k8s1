apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cluster2-certmanager
  namespace: argocd
spec:
  destination:
    server: https://kubernetes.default.svc
  project: default
  source:
    path: cluster2/argocd/cluster2-certmanager
    repoURL: ssh://git@gitea.c1.slow.moda:222/jr/k8s1.git
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
